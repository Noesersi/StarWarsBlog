{/* <div className="card" >
<div className="container">
      {targetResource ? (
        <div>
          <h2>{targetResource.name}</h2>
        {resourceType === "people" && <PeopleDetail {targetResource} />}
        {resourceType === "starships" && <StarshipDetail {targetResource} />} 
        {resourceType === "planets" && <PlanetDetail {targetResource} />}

          <br />
          <div className="card-body">
   <Link to="/">
            <button className="btn btn-primary">Back home</button>
          </Link>
  </div>
          
        </div>
      ) : (
        <p>Loading...</p>
      )}
    </div>
</div> */}